/**
 * @param {character[][]} board
 * @return {void} Do not return anything, modify board in-place instead.
 */
var solve = function(board) {
	// check the type




    
};

function recursion(board, i, j) {

	if(board[i][j] === 'X') {
		return;
	}


	board[i][j] = 'X';

	




	board[i][j] = 'O';
}